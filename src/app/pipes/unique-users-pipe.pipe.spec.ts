import { UniqueUsersPipePipe } from './unique-users-pipe.pipe';

describe('UniqueUsersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueUsersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
